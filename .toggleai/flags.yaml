version: 1
project: myapp

flags:
  new-checkout:
    name: "New Checkout Flow"
    type: boolean
    tags: [frontend, experiment]
    environments:
      staging:
        enabled: true
        rollout_percentage: 80
    variations:
      - key: control
        value: false
      - key: variant_a
        value: true

configs:
  api_timeout_ms:
    type: number
    environments:
