version: 1
project: myapp

flags:
  new-checkout:
    name: "New Checkout Flow"
    type: boolean
    tags: [frontend, experiment]
    environments:
      staging:
        enabled: true
        rollout_percentage: 100
      production:
        enabled: true
        rollout_percentage: 50
  new-theme:
    name: "New Theme"
    type: boolean
    environments:
      staging:
        enabled: true
        rollout_percentage: 50
      development:
        enabled: true
        rollout_percentage: 100
      production:
        enabled: true
        rollout_percentage: 100

configs:
  api_timeout_ms:
    type: number
    environments:
